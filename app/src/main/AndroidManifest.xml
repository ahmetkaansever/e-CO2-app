<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aksdev.projectsecondaryusage" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectPrimaryUsage" >
        <activity
            android:name=".MainPage"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DailyUsageMain">
        </activity>

        <activity android:name=".ProfilePage">
        </activity>

        <activity android:name=".MonthlyCalculator">
        </activity>

        <activity android:name=".PrimaryUsage">
        </activity>

        <activity android:name=".SecondaryUsage">
        </activity>

        <activity android:name=".AboutUs">
        </activity>

        <activity android:name=".Recycle">
        </activity>

        <activity android:name=".Solutions">
        </activity>
    </application>

</manifest>